# Production environment variables — committed (no secrets here)
# Vite bakes these into the bundle at build time (import.meta.env.VITE_*)

# Google Analytics 4 — theakcnse.com / GitHub Pages
VITE_GA_MEASUREMENT_ID=G-HTY7ZYLS9B

# Backend API URL — overridden by CI / Render dashboard if needed
# Leave blank to use the default from src/utils/api.js
VITE_API_URL=
